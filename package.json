{
  "name": "pocket-australia",
  "version": "1.0.0",
  "private": true,
  "description": "Bo & Bon's static pixel travel story. GitHub Pages needs only index.html, styles.css, app.js, data.js and assets/.",
  "scripts": {
    "dev": "vite",
    "check": "node --check app.js && node --check data.js && node --check assets/australia.js"
  },
  "devDependencies": {
    "vite": "7.3.1"
  }
}
